import { ChangeFontSize } from './change-font-size';

describe('ChangeFontSize', () => {
  it('should create an instance', () => {
    const directive = new ChangeFontSize();
    expect(directive).toBeTruthy();
  });
});
